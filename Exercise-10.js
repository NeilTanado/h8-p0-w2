console.log('Tugas 1')

var total = 0

for(var index = 1; index <= 100; index +=1){
    if(index%2===0){
      (total -= index);
    }else{(total += index);}}
    console.log(total)

  console.log('Tugas 2')

    var pagar = '';
    for(var index1 = 1; index1 <= 10; index1++) {
      for(var index2 = 0; index2 <= 10; index2++) {
     pagar = pagar+='#'}
     console.log(pagar);
    }


console.log('Tugas 3')

    var bintang = '';
    for(var index1 = 1; index1 <= 10; index1++) {
      for(var index2 = 0; index2 <= 10; index2++) {
     bintang = bintang+='*'}
     console.log(bintang);
    }
